<% content_for :title, "#{@account.name}: View Unbound Account Entry" %>

<h1><%= @account.name %>: View Unbound Account Entry</h1>

<%= render :partial => 'account_entries/show', :locals => {:account_entry => @unbound_account_entry} %>
